import HomePageContent from "@/components/HomePageContent";

/** Public marketing homepage (moderators logged in land on `/` as the dashboard). */
export default function ExplorePage() {
  return <HomePageContent />;
}
